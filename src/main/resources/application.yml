spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/DevBlog
    username: root
    password: root

  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: validate

application:
  cron-task:
    deleteSmallArticlesCron: "0 0 0 * * *"